<% if flash.any? %>
  <div class='a-flash__box'>
    <div class='a-flash__message'>
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :class => "a-flash--#{name}" if msg.is_a?(String) %>
      <% end %>
    </div>
  </div>
<% end %>
